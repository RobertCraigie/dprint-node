import { Options } from './options';

export function format(filePath: string, code: string, options?: Options): string;

// TODO
export function formatMarkdown(code: string, options?: Record<string, unknown>): string;
